syntax = "proto3";

import "header.proto";

package sensors;

enum CameraEncoding {
    UNKNOWN = 0;
    RGB8 = 1;
    BGR8 = 2;
    GRAY8 = 3;
    MJPEG = 4;
}

message CameraStreamRequest {
}

message CameraStreamReply {
    Header header = 1;
    uint32 width = 2;
    uint32 height = 3;
    CameraEncoding encoding = 4;
    bytes image_data = 5;
}

service CameraService {
    rpc getCameraFrame(CameraStreamRequest) returns (stream CameraStreamReply);
}
