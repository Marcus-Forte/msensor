syntax = "proto3";

import "header.proto";

package sensors;

message AdcDataRequest {
    optional uint32 channel = 1;
}

message AdcData {
    Header header = 1;
    float sample = 2;
}

service AdcService {
    rpc getAdcData(AdcDataRequest) returns (AdcData);
}
